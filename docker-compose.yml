services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_CONFIG: development
    ports:
      - "4200:80"
    container_name: vehicle-detection-frontend
    restart: unless-stopped
